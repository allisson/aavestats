# Per-chain RPC endpoints (server-side only — never exposed to the client).
# If left unset, viem falls back to the chain's public RPC (rate-limited).
RPC_URL_ARBITRUM=
RPC_URL_ETHEREUM=
RPC_URL_OPTIMISM=
RPC_URL_BASE=
RPC_URL_POLYGON=
